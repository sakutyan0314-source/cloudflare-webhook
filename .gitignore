# Node / npm
node_modules/
.npm/

# Wrangler / Cloudflare local files
.wrangler/
.wrangler_deploy_safe/
.dev.vars*
.env*

# Google service-account credentials (must remain outside this repository)
service-account*.json
*service-account-key*.json
google-service-account*.json

# macOS
.DS_Store
Library/

# Local backup files
*.before-cloudflare-sync-*

# Logs
*.log

# Phase 1B local-only human review ledger (the implementation also rejects
# repository paths; these patterns prevent accidental staging as a second guard).
topic-candidate-ledger/
*.topic-candidate-ledger.jsonl
